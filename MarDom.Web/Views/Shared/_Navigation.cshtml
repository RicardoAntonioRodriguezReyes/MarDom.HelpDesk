<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                   
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold">@User.Identity.Name</strong>
                            </span> <span class="text-muted text-xs block"></span>
                        </span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li><a href="@Url.Action("Index", "Manage")">Perfil</a></li>

                        <li class="divider"></li>
                        <li><a href="@Url.Action("Login", "Pages")">Logout</a></li>
                    </ul>
                </div>
                <div class="logo-element">
                    HD
                </div>
            </li>
     

            <li class="@Html.IsSelected(controller: "Retail")">
                <a href="@Url.Action("Upload", "Retail")"><i class="fa fa-folder-open"></i> <span class="nav-label" data-i18n="nav.dashboard">Solicitudes</span> <span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse @Html.IsSelected(controller: "Retail", cssClass: "in")">
                    <li class="@Html.IsSelected(action: "Upload")"><a href="@Url.Action("Upload", "Retail")">Crear solicitud</a></li>
                    <li class="@Html.IsSelected(action: "ValidityRetail")"><a href="@Url.Action("ValidityRetail", "Retail")">Solicitud</a></li>
                </ul>
            </li>
            <li class="@Html.IsSelected(controller: "Report")">
                <a href="@Url.Action("Index", "Report")"><i class="fa fa-line-chart"></i> <span class="nav-label" data-i18n="nav.dashboard">Informes</span> <span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse @Html.IsSelected(controller: "Report", cssClass: "in")">
                    <li class="@Html.IsSelected(action: "Earnings")"><a href="@Url.Action("Earnings", "Report")">Histórico de solicitudes</a></li>
                    <li class="@Html.IsSelected(action: "Earnings")"><a href="@Url.Action("Earnings", "Report")">Solicitudes por parametros</a></li>
                    <li class="@Html.IsSelected(action: "Sales")"><a href="@Url.Action("Sales", "Report")">Tiempo de gestión de las solicitudes</a></li>
                    <li class="@Html.IsSelected(action: "Sales")"><a href="@Url.Action("Sales", "Report")">Solicitudes cerradas</a></li>
                </ul>
            </li>
           
        </ul>
    </div>
</nav>

